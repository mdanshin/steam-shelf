{
  "name": "steam-shelf",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24.0.0" },
  "scripts": {
    "start": "node --env-file-if-exists=.env.local server.js",
    "test": "python -m unittest discover -s test -p \"test_*.py\" && node --test",
    "check": "node --check server.js && node --check public/app.js && node --check public/catalog-lifecycle.js && node --check lib/config.js && node --check lib/google-oauth.js && node --check lib/secrets.js && node --check lib/steam-api.js && node --check lib/store.js && python scripts/verify_pages.py",
    "sync:deals": "python scripts/sync_deals.py"
  }
}
