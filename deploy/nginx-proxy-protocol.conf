# Insert these directives inside the existing api.danshin.ms TLS server.
# Port 8443 remains as an immediate xver=0 rollback target.
listen 127.0.0.1:8444 ssl proxy_protocol;
set_real_ip_from 127.0.0.1;
real_ip_header proxy_protocol;
